# Inbox ordering example

## Signal from session
The user corrected the agent twice:

1. `INGEST` should be interpreted as applying **only to `_Inbox/` files**.
2. "The first two files" should follow the user's **date-ascending visible order**, not generic search output order.

## Concrete file pair from the session
The intended pair was:
- `Hermes × Clay OAuth Connection Runbook for AI Agents`
- `This OpenSource Repo will 10X Your Hermes Agent-Jack Roberts 23-06-2026`

## Durable lesson
When Robert uses positional inbox language, resolve the set from `_Inbox/` first, then apply his stated ordering convention. If a tool's ordering differs from his interface ordering, his ordering wins.
