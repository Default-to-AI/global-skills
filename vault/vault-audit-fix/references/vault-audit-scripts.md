# Vault Audit Scripts

Run from `C:\Users\Tiger\Vault`.

```bash
python scripts/check_vault.py --quick --json
python scripts/build_catalog.py --stats
python scripts/build_index.py --check
```

If a command is unavailable or arguments differ, inspect `scripts/` and use the live script help/source rather than guessing.
