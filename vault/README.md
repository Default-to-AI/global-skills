# Obsidian Vault Skills

This folder contains the dedicated Obsidian/Vault operation skills under HERMES_HOME/skills.

The active vault is:

`C:\Users\Tiger\Vault`

Use these skills as focused procedures:

- `vault-umbrella` — entrypoint and loader map.
- `vault-ingestion` — canonical source ingestion for foreground, on-demand, and background workflows.
- `vault-audit-fix` — run full audit, fix safe findings, report approval-needed items.
- `vault-retrieval` — answer from existing vault material without ingesting new sources.
- `vault-graph-hygiene` — links, orphan handling, duplicate candidates, merge/rename gates.
- `vault-compounding-loop` — decide when durable outputs should or should not be created.
- `vault-external-workflow-evaluation` — evaluate external tools/repos/workflows before adapting them.
- `vault-cron-ingestion` — cron-safe wrapper around canonical ingestion.

Do not install external Obsidian schemas into Robert's vault without explicit approval. Live vault files remain authoritative: `vault-guide.md`, `STANDARDS.md`, `vault-index.md`, domain `wiki/AGENTS.md`, and domain `wiki/log.md`.
